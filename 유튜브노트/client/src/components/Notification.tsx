import React from "react";

// 이 컴포넌트는 상태를 직접 관리하지 않음
// 모든 알림은 Shadcn의 Toaster 컴포넌트를 통해 처리
const Notification: React.FC = () => {
  // 빈 컴포넌트 반환 (Toaster가 App.tsx에서 이미 렌더링됨)
  return null;
};

export default Notification;
